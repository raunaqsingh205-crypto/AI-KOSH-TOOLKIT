# API & Integration Discrepancies Log

| ID | Component/Route | Mismatched Files & Line Numbers | Description of Discrepancy | Backend Expectation | Frontend Implementation / Draft | Proposed Resolution |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `D-002` | `POST /api/v1/assess` | [assessment.py:L16](file:///Users/prabhavtrivedi/Desktop/AI-KOSH-TOOLKIT/backend/app/schemas/assessment.py#L16) <br> [use-assessment.ts:L40](file:///Users/prabhavtrivedi/Desktop/AI-KOSH-TOOLKIT/frontend/hooks/use-assessment.ts#L40) | The backend API schema does not accept an `assessment_mode` parameter (e.g. `lite` vs `full`), hardcoding `"full"`, while database structures and PRD suggest supporting dual-mode runs. | Hardcodes `assessment_mode="full"` during submission insertion. | Defaults to submitting a single generic run without mode selector. | Keep hardcoded `"full"` mode runs for the automated toolkit, documenting it as the unified standard assessment, avoiding unnecessary schema fragmentation. |
| `D-003` | `/register` (User Account Creation) | [auth.py:L29](file:///Users/prabhavtrivedi/Desktop/AI-KOSH-TOOLKIT/backend/app/api/v1/auth.py#L29) <br> [register/page.tsx:L20](file:///Users/prabhavtrivedi/Desktop/AI-KOSH-TOOLKIT/frontend/app/(auth)/register/page.tsx#L20) | Frontend password validation schema is too permissive (min 6 characters) compared to the backend's strict password complexity regex (min 8 characters, uppercase, lowercase, digit, special character). | Expects a complex password matching the regex: `^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$`. | Validates password with `z.string().min(6)`. | Align the frontend Zod schema in `register/page.tsx` using a regex pattern matching the backend complexity rules. |

